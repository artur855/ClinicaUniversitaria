matrix:
  include:
    - language: node_js
      before_script:
        - cd front
      
      addons:
        apt:
          sources:
            - google-chrome
          packages:
            - google-chrome-stable
      
      cache:
        directories:
        - ./node_modules  
      
      install:
        - npm install
      
      script:
        - npm test
